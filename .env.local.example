NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SENDGRID_API_KEY=
FROM_EMAIL=
APP_URL=
REPORT_ACTION_SECRET=

# PayPlus — server-side only. NEVER expose via NEXT_PUBLIC_.
# Used by /api/orders/[id]/create-payment-link to mint a payment-page URL
# per order. Browser only ever receives the resulting URL — not the keys.
PAYPLUS_API_KEY=
PAYPLUS_SECRET_KEY=
PAYPLUS_PAYMENT_PAGE_UID=
